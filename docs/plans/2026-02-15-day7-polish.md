# Day 7: UI Polish & Finalization Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Polish the user experience with rich demo data and refined visuals.

**Architecture:** Load default YAML on mount -> Editor displays it -> Graph renders automatically.

**Tech Stack:** React, Tailwind.

---

### Task 1: Create Default Demo Data

**Files:**
- Create: `src/features/editor/data/default-yaml.ts`

**Step 1: Create Demo Data**

Create `src/features/editor/data/default-yaml.ts`.

```typescript
export const DEFAULT_YAML = `version: '3.8'

services:
  # Frontend Application
  frontend:
    image: node:18-alpine
    container_name: composeviz-web
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - API_URL=http://backend:8000
    depends_on:
      - backend
    networks:
      - app-net

  # Backend API
  backend:
    image: python:3.9-slim
    command: uvicorn main:app --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"
    environment:
      - DB_HOST=db
      - REDIS_HOST=redis
    depends_on:
      - db
      - redis
    networks:
      - app-net

  # Database
  db:
    image: postgres:14
    volumes:
      - db_data:/var/lib/postgresql/data
    environment:
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=secret
      - POSTGRES_DB=app_db
    networks:
      - app-net

  # Cache
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
    networks:
      - app-net

  # Monitoring
  prometheus:
    image: prom/prometheus
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
    ports:
      - "9090:9090"
    networks:
      - app-net

networks:
  app-net:
    driver: bridge

volumes:
  db_data:
`;
```

**Step 2: Commit**

```bash
git add src/features/editor/data/default-yaml.ts
git commit -m "feat(polish): add rich default demo yaml"
```

---

### Task 2: Load Demo on Mount

**Files:**
- Modify: `src/features/editor/components/Editor.tsx`

**Step 1: Update Editor**

Update `src/features/editor/components/Editor.tsx` to use `DEFAULT_YAML` as the initial state.

```typescript
import { DEFAULT_YAML } from '../data/default-yaml';

// In Editor component:
const [code, setCode] = useState(DEFAULT_YAML); // Initialize with demo
```

**Step 2: Commit**

```bash
git add src/features/editor/components/Editor.tsx
git commit -m "feat(polish): load demo data on mount"
```

---

### Task 3: Polish UI

**Files:**
- Modify: `src/features/editor/components/Editor.tsx`

**Step 1: Improve Error Display**

Make the error message more prominent (e.g. red background banner).

```typescript
// Inside render:
<div className="flex-1 relative flex flex-col">
  {error && (
    <div className="bg-destructive/10 border-b border-destructive/20 px-4 py-2 text-xs text-destructive flex items-center gap-2">
      <span className="font-bold">Error:</span>
      <span className="font-mono">{error.split('\n')[0]}</span>
    </div>
  )}
  <textarea
    // ...
    className={cn(
      "w-full h-full p-4 font-mono text-sm bg-background resize-none focus:outline-none",
      "text-foreground placeholder:text-muted-foreground/50",
      // Remove border-l-2 logic here since we have a banner now
    )}
    // ...
  />
</div>
```

**Step 2: Commit**

```bash
git add src/features/editor/components/Editor.tsx
git commit -m "feat(polish): improve error display"
```

---

### Task 4: Final Verification

**Files:**
- None

**Step 1: Run Full Build**

Run `npm run build`.

**Step 2: Report Success**
